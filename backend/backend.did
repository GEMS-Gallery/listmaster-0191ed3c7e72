type ShoppingItem = 
 record {
   completed: bool;
   description: text;
   id: nat;
 };
service : {
  addItem: (text) -> (nat);
  deleteItem: (nat) -> (bool);
  getItems: () -> (vec ShoppingItem) query;
  getPredefinedProducts: () -> (vec text) query;
  toggleItem: (nat) -> (bool);
}
